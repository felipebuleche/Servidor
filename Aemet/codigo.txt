if (isset($xml)) {
    $weatherData = simplexml_load_string($xml);

    foreach ($weatherData->prediccion->dia as $day) {
        $fecha = $day["fecha"];
        $maxTemp = $day->temperatura->maxima;
        $minTemp = $day->temperatura->minima;

        echo "Día: $fecha<br>";
        echo "Temperatura Máxima: $maxTemp °C<br>";
        echo "Temperatura Mínima: $minTemp °C<br><br>";
    }
}


if (isset($xml)) {
            $weatherData = simplexml_load_string($xml);
            $dia = $weatherData->prediccion->dia["fecha"];
            $maxTemp = $weatherData->prediccion->dia->temperatura->maxima;
            $minTemp = $weatherData->prediccion->dia->temperatura->minima;

            
            echo "Municipio: $ciudad<br>";
            echo "Día: $dia<br>";
            echo "Temperatura Máxima: $maxTemp °C<br>";
            echo "Temperatura Mínima: $minTemp °C<br>";
        }